# Frog in the Cloud

Cloud-run crawler control plane with local and staging-backed development modes.

## Development Startup

For the current development startup, recovery, and side-by-side local/staging workflow, see:

- `docs/development/dev-startup.md`

Quick links:

- Local API: `http://localhost:8000`
- Local UI: `http://localhost:3001/crawls`
- Staging-backed UI: `http://localhost:3002/crawls`
- Frontend deploy runbook: `docs/development/web-deploy.md`
